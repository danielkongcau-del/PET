# The PyTorch stack was installed manually for the RTX 5070 Ti (Blackwell).
# Keep these exact local CUDA wheels immutable while resolving all other packages.
torch==2.10.0+cu130
torchvision==0.25.0+cu130
torchaudio==2.10.0+cu130

# Compatibility bridge for the 2023-2024 research repositories.
numpy==1.26.4
pillow==10.4.0
protobuf==3.20.3
packaging==24.1
fsspec==2024.10.0
setuptools==80.9.0
jsonschema==4.23.0

# Live2Diff targets diffusers 0.25.0.  huggingface-hub 0.25.x is the last
# generation that still exposes APIs used by diffusers 0.24/0.25-era code.
diffusers==0.25.0
huggingface-hub==0.25.2
transformers==4.38.2
tokenizers==0.15.2
accelerate==0.27.2
peft==0.9.0
safetensors==0.4.5

# Lightning 2.1 still imports pkg_resources, which setuptools 81+ removed.
lightning==2.1.4
pytorch-lightning==2.1.4
torchmetrics==1.3.2
